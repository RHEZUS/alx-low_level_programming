#include "main.h"

/**
 * mul - miltiplies two integers
 * @a: the first number
 * @b: the second number
 * Return: the result of the multiplication
 */

int mul(int a, int b);

int mul(int a, int b)
{
	return (a * b);
}
