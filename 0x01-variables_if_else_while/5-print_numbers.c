#include <stdio.h>

/**
 * main - prints the base 10 single digit numbers
 * Return: 0
 */

int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
