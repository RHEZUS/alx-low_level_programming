#include <stdio.h>
/**
 * main - Display a message
 * Return: return 0 if message is printed elsewayr return something else
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
